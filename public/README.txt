Drop static assets here. The app references:

  resume.pdf    -> "Download Resume" button (lib/data.ts -> profile.resume)
  gamerpic.png  -> optional avatar image (currently the "TB" initials block)

Anything in /public is served from the site root, e.g. /resume.pdf
